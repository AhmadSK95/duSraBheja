# Database
DATABASE_URL=postgresql+asyncpg://brain_user:changeme@db-host:25060/brain_db
DB_PASSWORD=changeme
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=5
DB_STATEMENT_TIMEOUT_MS=30000
DB_SSL=true
DB_SSL_REQUIRE=true
DB_SSL_REJECT_UNAUTHORIZED=false

# Redis
REDIS_URL=redis://localhost:6379

# Discord
DISCORD_TOKEN=your_discord_bot_token
DISCORD_GUILD_ID=your_numeric_guild_id
INBOX_CHANNEL_NAME=inbox
NEEDS_REVIEW_CHANNEL_NAME=needs-review
DAILY_DIGEST_CHANNEL_NAME=daily-digest
ASK_CHANNEL_NAME=ask-brain

# Anthropic (Claude)
ANTHROPIC_API_KEY=your_anthropic_key

# OpenAI (embeddings + Whisper)
OPENAI_API_KEY=your_openai_key

# Private API
API_TOKEN=replace_with_long_random_token
API_HOST=0.0.0.0
API_PORT=8000

# MCP
MCP_TRANSPORT=streamable-http
MCP_PORT=8100

# Classification
CONFIDENCE_THRESHOLD=0.75
MAX_CLARIFICATION_ATTEMPTS=2
CHUNK_MAX_TOKENS=512
CHUNK_OVERLAP_TOKENS=64

# Blob storage
BLOB_STORAGE_PATH=/data/blobs

# Digest
DIGEST_CRON_HOUR=8
DIGEST_TIMEZONE=America/New_York

# Optional GitHub sync
GITHUB_API_TOKEN=
GITHUB_API_BASE_URL=https://api.github.com

# Optional collector
COLLECTOR_DEVICE_NAME=macbook
COLLECTOR_INTERVAL_HOURS=24
COLLECTOR_PROJECT_ROOTS=/abs/path/to/daily/root
COLLECTOR_BOOTSTRAP_ROOTS=/Users/you/Desktop,/Users/you/Documents,/Users/you/Downloads
COLLECTOR_DAILY_ROOTS=/Users/you/Desktop
COLLECTOR_STATE_PATH=~/.brain-collector/state.json
# Keep the API private and forward localhost:8000 over SSH with scripts/open_collector_tunnel.sh.
COLLECTOR_API_BASE_URL=http://127.0.0.1:8000
COLLECTOR_SCAN_MAX_DEPTH=4
COLLECTOR_INVENTORY_RECENT_FILES_LIMIT=50
